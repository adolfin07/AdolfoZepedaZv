<nav
  class="fixed z-50 w-screen backdrop-blur-md bg-white/10 border-b border-white/20"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <!-- logo -->
      <div class="flex items-center space-x-3">
        <a href="#about">
          <img src="/img/Z.png" alt="Logo" class="h-8 rounded-sm" />
        </a>
      </div>

      <!-- boton movil -->
      <button
        id="menu-btn"
        aria-label="Toggle menu"
        class="md:hidden p-2 rounded-md text-white hover:bg-white/10"
      >
        <i class="fa-solid fa-bars"></i>
      </button>

      <!-- menu desktop -->
      <div class="hidden md:flex gap-8">
        <a href="#about" class="text-white hover:text-red-400 transition"
          >About</a
        >
        <a href="#projects" class="text-white hover:text-red-400 transition"
          >Projects</a
        >
        <a href="#experience" class="text-white hover:text-red-400 transition"
          >Experience</a
        >
        <a href="#skills" class="text-white hover:text-red-400 transition"
          >Skills</a
        >
      </div>
    </div>
  </div>

  <!-- mobile menu -->
  <div
    id="mobile-menu"
    class="hidden fixed top-0 left-0 h-screen w-64 bg-black/80 backdrop-blur-md z-40 flex flex-col pt-20 px-6 space-y-6 transform -translate-x-full transition-transform duration-300"
  >
    <a href="#about" class="text-white text-lg hover:text-red-400">About</a>
    <a href="#projects" class="text-white text-lg hover:text-red-400"
      >Projects</a
    >
    <a href="#experience" class="text-white text-lg hover:text-red-400"
      >Experience</a
    >
    <a href="#skills" class="text-white text-lg hover:text-red-400">Skills</a>
  </div>

  <div id="overlay" class="hidden fixed inset-0 bg-black/50 z-30"></div>
</nav>
