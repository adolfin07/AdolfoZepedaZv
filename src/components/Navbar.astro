<nav class="fixed z-50 w-screen bg-[var(--deep-blue)] shadow-md">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <!-- logo -->
      <div class="flex items-center space-x-3">
        <img src="/img/Z.png" alt="Logo" class="h-8 rounded-sm" />
      </div>

      <!-- boton movil -->
      <button id="menu-btn" aria-label="Toggle menu"
        class="md:hidden inline-flex items-center justify-center p-2 rounded-md text-white hover:bg-[var(--blue)] focus:outline-none">
        <svg id="menu-open" class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none"
          viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
        <svg id="menu-close" class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none"
          viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>

      <!-- menu desktop -->
      <div id="menu" class="hidden md:flex md:items-center md:space-x-8">
        <a href="#about" class="text-white hover:text-[var(--red)]">About</a>
        <a href="#projects" class="text-white hover:text-[var(--red)]">Projects</a>
        <a href="#experience" class="text-white hover:text-[var(--red)]">Experience</a>
        <a href="#skills" class="text-white hover:text-[var(--red)]">Skills</a>
      </div>
    </div>
  </div>

  <!-- menu movil -->
  <div id="mobile-menu"
    class="hidden fixed top-0 left-0 h-screen w-64 bg-[var(--dark-blue)] z-40 flex flex-col pt-20 px-6 space-y-6 transform -translate-x-full transition-transform duration-300">
    <a href="#about" class="text-white font-medium hover:text-[var(--red)] text-lg">About</a>
    <a href="#projects" class="text-white font-medium hover:text-[var(--red)] text-lg">Projects</a>
    <a href="#experience" class="text-white font-medium hover:text-[var(--red)] text-lg">Experience</a>
    <a href="#skills" class="text-white font-medium hover:text-[var(--red)] text-lg">Skills</a>
  </div>

  <!-- overlay -->
  <div id="overlay" class="hidden fixed inset-0 bg-black/50 z-30"></div>
</nav>
